interface:
  display_name: "Bootstrap WSL AI Dev"
  short_description: "Build the WSL foundation before server extensions"
  default_prompt: "Use $bootstrap-wsl-ai-dev to audit and configure the WSL foundation before invoking $bootstrap-wsl-server."
